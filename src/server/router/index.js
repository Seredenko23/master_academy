const task = require('./task');
const sales = require('./sales');
const products = require('./products');
const authorization = require('./authorization');

module.exports = { task, sales, products, authorization };
